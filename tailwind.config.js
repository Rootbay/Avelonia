/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind v4 uses content discovery via the bundler,
  // so explicit `content` is not required here.
  theme: {
    extend: {},
  },
};
