*** Begin Patch
*** Update File: src/lib/components/DownloadItem.svelte
@@
-  class="data-[status=downloading]:border-l-2 data-[status=downloading]:border-primary
+  class="relative data-[status=downloading]:border-l-2 data-[status=downloading]:border-primary
          data-[status=pending]:border-l-2 data-[status=pending]:border-primary/70
          data-[status=queued]:border-l-2 data-[status=queued]:border-primary/70
          data-[status=failed]:border-l-2 data-[status=failed]:border-destructive"
 >
@@
-      <div class="flex flex-nowrap gap-2 text-xs text-muted-foreground md:hidden overflow-x-auto">
+      <div class="flex flex-nowrap gap-2 text-xs text-muted-foreground md:hidden overflow-x-auto">
         <span>{download.size}</span>
         {#if download.fileType}<span>- {download.fileType}</span>{/if}
         {#if download.category}<span>- {download.category}</span>{/if}
-        {#if download.eta}<span>- {download.eta}</span>{/if}
+        <!-- ETA removed from compact view -->
       </div>
     </div>
   </TableCell>
@@
-  <TableCell class="hidden md:table-cell text-muted-foreground">
-    {download.category}
-  </TableCell>
-
-  <TableCell class="hidden md:table-cell text-muted-foreground">
-    {download.eta}
-  </TableCell>
+  <TableCell class="hidden md:table-cell text-muted-foreground">
+    {download.category}
+  </TableCell>
@@
-    <span class={	ext-xs font-semibold uppercase tracking-wide }>
+    <span class={	ext-xs font-semibold uppercase tracking-wide }>
       {download.status}
       {#if download.progress < 0}
         <span class="ml-1 text-muted-foreground">(preparing)</span>
       {/if}
-      {#if download.status === 'downloading' && download.speed}
-        <span class="ml-1 text-muted-foreground">- {download.speed}</span>
-      {/if}
+      <!-- speed removed to avoid noisy updates -->
     </span>
@@
-  {#if download.status === 'downloading' || download.status === 'pending'}
-    <TableCell colspan={7}>
-      <Progress
-        indeterminate={download.progress < 0}
-        value={download.progress < 0 ? undefined : Math.floor(download.progress)}
-        max={100}
-        aria-label="Download progress"
-      />
-    </TableCell>
-  {/if}
+  <!-- Bottom underline progress indicator -->
+  <div class="pointer-events-none absolute inset-x-0 bottom-0 h-[2px]">
+    {#if download.status === 'completed'}
+      <div class="h-full w-full bg-emerald-500"></div>
+    {:else if download.status === 'downloading' && download.progress >= 0}
+      <div
+        class="h-full bg-primary transition-[width] duration-300 ease-out"
+        style={width:%}
+      ></div>
+    {:else if download.status === 'pending' || (download.status === 'downloading' && download.progress < 0)}
+      <div class="relative h-full overflow-hidden">
+        <div class="absolute left-0 top-0 h-full w-1/3 bg-primary/60 dl-underline-indeterminate"></div>
+      </div>
+    {/if}
+  </div>
 </TableRow>
 
 <style>
   @keyframes dl-underline-indeterminate {
     0% { transform: translateX(-100%); }
     100% { transform: translateX(300%); }
   }
   .dl-underline-indeterminate { animation: dl-underline-indeterminate 1.6s linear infinite; }
 </style>
*** End Patch
